function Car(make, model, year) {
    this.make = make;
    this.model = model;
    this.year = year;
  }
  const car1 = new Car('Eagle', 'Talon TSi', 1993);
  const car2 = new Car('EGL', 'ford', 2004);
  console.log(car1);
  console.log(car2);

//

// class Car {
//     constructor(make, model, year) {
//         this.make = make;
//         this.model = model;
//         this.year = year;
//     }
//   }
//   mycar = new Car('Eagle', 'Talon TSi', 1993);

// console.log(mycar);